---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: invorainvoicingregulations.invoicing.invora.app
spec:
  group: invoicing.invora.app
  names:
    kind: InvoraInvoicingRegulation
    listKind: InvoraInvoicingRegulationList
    plural: invorainvoicingregulations
    shortNames:
    - ireg
    singular: invorainvoicingregulation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.regulationType
      name: Type
      type: string
    - jsonPath: .spec.submissionModel
      name: Model
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.enrollmentStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InvoraInvoicingRegulation represents regulation enrollment config
          for a branch.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              InvoraInvoicingRegulationSpec defines regulation enrollment config for a branch.
              Each regulation plugin (ZATCA, ETA Egypt, Peppol) has its own enrollment
              requirements and credentials.
            properties:
              branchRef:
                description: BranchRef references the InvoraBranch this regulation
                  applies to.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              config:
                additionalProperties:
                  type: string
                description: Config holds regulation-specific configuration as key-value
                  pairs.
                type: object
              credentialsRef:
                description: |-
                  CredentialsRef references the Secret containing regulation-specific
                  credentials (e.g., ZATCA CSID certificate, ETA client credentials).
                properties:
                  key:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - key
                - name
                type: object
              enabled:
                default: true
                description: Enabled controls whether this regulation is actively
                  enforced.
                type: boolean
              instanceRef:
                description: InstanceRef references the InvoraBillingInstance (gateway
                  connection).
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              regulationType:
                description: RegulationType identifies the regulation plugin.
                enum:
                - zatca
                - eta_egypt
                - peppol_sa
                - peppol_ae
                - peppol_be
                - peppol_de
                - peppol_sg
                - jordan
                - uae
                type: string
              submissionModel:
                description: SubmissionModel defines how documents are submitted to
                  the authority.
                enum:
                - clearance
                - network_delivery
                - reporting
                type: string
            required:
            - branchRef
            - instanceRef
            - regulationType
            type: object
          status:
            description: InvoraInvoicingRegulationStatus defines the observed state.
            properties:
              conditions:
                description: Conditions represent the latest observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              enrollmentStatus:
                description: EnrollmentStatus reflects the backend enrollment state.
                type: string
              lastSyncedAt:
                description: LastSyncedAt is the last time this resource was synced.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last spec generation the controller
                  acted on.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
