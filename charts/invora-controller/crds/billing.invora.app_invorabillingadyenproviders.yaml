---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: invorabillingadyenproviders.billing.invora.app
spec:
  group: billing.invora.app
  names:
    kind: InvoraBillingAdyenProvider
    listKind: InvoraBillingAdyenProviderList
    plural: invorabillingadyenproviders
    shortNames:
    - iadyen
    singular: invorabillingadyenprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.code
      name: Code
      type: string
    - jsonPath: .spec.merchantAccount
      name: Merchant
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InvoraBillingAdyenProvider represents an Adyen payment provider configured
          on a billing organization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              InvoraBillingAdyenProviderSpec defines the desired state of an Adyen payment
              provider attached to a billing organization.
            properties:
              apiKeyRef:
                description: ApiKeyRef references the Kubernetes Secret holding the
                  Adyen API key.
                properties:
                  key:
                    description: Key within the Secret data.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. Defaults to the referencing
                      resource's namespace.
                    type: string
                required:
                - key
                - name
                type: object
              code:
                description: Code is the provider code. Immutable after creation.
                minLength: 1
                type: string
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy determines what happens to the billing resource when this
                  CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              hmacKeyRef:
                description: |-
                  HmacKeyRef references the Kubernetes Secret holding the Adyen HMAC key
                  for webhook signature verification.
                properties:
                  key:
                    description: Key within the Secret data.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. Defaults to the referencing
                      resource's namespace.
                    type: string
                required:
                - key
                - name
                type: object
              livePrefix:
                description: LivePrefix is required for live (non-test) Adyen environments.
                type: string
              merchantAccount:
                description: MerchantAccount is the Adyen merchant account identifier.
                minLength: 1
                type: string
              name:
                description: Name is the display name shown in the billing UI.
                minLength: 1
                type: string
              organizationRef:
                description: |-
                  OrganizationRef references the InvoraBillingOrganization that owns this
                  payment provider.
                properties:
                  name:
                    description: Name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced resource. Defaults to the namespace
                      of the referencing resource when omitted.
                    type: string
                required:
                - name
                type: object
              successRedirectUrl:
                description: SuccessRedirectUrl is the URL for successful payments.
                type: string
            required:
            - apiKeyRef
            - code
            - merchantAccount
            - name
            - organizationRef
            type: object
          status:
            description: InvoraBillingAdyenProviderStatus defines the observed state.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              id:
                description: ID is the billing-backend-assigned resource identifier.
                type: string
              lastSyncedAt:
                description: |-
                  LastSyncedAt is the last time the controller successfully reconciled
                  the resource against the billing API.
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent .metadata.generation observed
                  by the controller.
                format: int64
                type: integer
              providerCode:
                description: ProviderCode is the resolved billing provider code.
                type: string
              providerId:
                description: ProviderID is the billing-backend-assigned UUID for this
                  Adyen provider.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
