---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: invorabillingmetrics.billing.invora.app
spec:
  group: billing.invora.app
  names:
    kind: InvoraBillingMetric
    listKind: InvoraBillingMetricList
    plural: invorabillingmetrics
    shortNames:
    - lbm
    singular: invorabillingmetric
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.code
      name: Code
      type: string
    - jsonPath: .spec.aggregationType
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InvoraBillingMetric represents a billable metric in a billing
          organization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InvoraBillingMetricSpec defines a usage measurement definition
              in billing.
            properties:
              aggregationType:
                description: AggregationType defines how events are aggregated.
                enum:
                - count_agg
                - sum_agg
                - max_agg
                - unique_count_agg
                - latest_agg
                - weighted_sum_agg
                - custom_agg
                type: string
              code:
                description: Code is the unique identifier for this metric. Immutable
                  after creation.
                minLength: 1
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy determines what happens to the billing
                  resource when this CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              description:
                description: Description is optional.
                type: string
              fieldName:
                description: FieldName is the event property to aggregate. Required
                  for sum_agg, max_agg, etc.
                type: string
              filters:
                description: |-
                  Filters defines property-based filter keys and their allowed values for
                  billing charge filter matching. Events carry these properties; plan charges
                  use ChargeFilter to price each combination independently.
                items:
                  description: |-
                    BillableMetricFilter defines a property-based filter key and its allowed values
                    for billing charge filter matching.
                  properties:
                    key:
                      description: Key is the event property name used for filtering
                        (e.g. "document_type", "regulation_id").
                      minLength: 1
                      type: string
                    values:
                      description: Values are the allowed values for this filter key.
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - key
                  - values
                  type: object
                type: array
              name:
                description: Name is the display name.
                minLength: 1
                type: string
              organizationRef:
                description: OrganizationRef references the InvoraBillingOrganization
                  this metric belongs to.
                properties:
                  name:
                    description: Name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced resource. Defaults to the namespace
                      of the referencing resource when omitted.
                    type: string
                required:
                - name
                type: object
              recurring:
                description: Recurring indicates if the metric is recurring across
                  billing periods.
                type: boolean
              weightedInterval:
                description: WeightedInterval for weighted_sum_agg (e.g. "seconds").
                type: string
            required:
            - aggregationType
            - code
            - name
            - organizationRef
            type: object
          status:
            description: InvoraBillingMetricStatus defines the observed state of a
              InvoraBillingMetric.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalId:
                description: ExternalID is the billing-backend-assigned UUID.
                type: string
              id:
                description: ID is the billing-backend-assigned resource identifier.
                type: string
              lastSyncedAt:
                description: |-
                  LastSyncedAt is the last time the controller successfully reconciled
                  the resource against the billing API.
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent .metadata.generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
